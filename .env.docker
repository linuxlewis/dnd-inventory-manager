# D&D Inventory Manager - Docker Production Environment
# Copy to .env.docker and customize as needed

# Database (SQLite path inside container, data volume mounted)
DATABASE_URL=sqlite+aiosqlite:///./data/dnd_inventory.db

# CORS Origins (add your Tailscale IP if needed)
# Example: CORS_ORIGINS=["http://localhost:9080","http://100.x.x.x:9080"]
CORS_ORIGINS=["http://localhost:9080","http://localhost:5173"]

# Backend port (internal, mapped to 9000 externally)
PORT=8000
